# Email Masking Script

This script contains a single function called `mask()`. It takes an email address and hides (masks) part of the username for privacy.

**Example:** `user@example.com` → `u**r@example.com`

The first and last letters stay visible. Everything in between becomes `*`.

---

